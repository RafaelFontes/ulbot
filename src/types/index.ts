const TYPES = {
    CONFIG_INTERFACE: Symbol.for('ConfigInterface'),
    RESOURCE_LOADER: Symbol.for('ResourceLoader'),
    LOGGER_INTERFACE: Symbol.for('LoggerInterface'),
    VERSION_PLUGIN: Symbol.for('VersionPlugin'),
};

export default TYPES;